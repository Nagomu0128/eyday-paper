{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "eyday-paper",
  "main": "src/index.ts",
  "compatibility_date": "2025-10-01",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "binding": "ASSETS",
    "not_found_handling": "single-page-application",
    "run_worker_first": ["/api/*"]
  },
  "observability": {
    "enabled": true
  },
  // Relational data (Better Auth tables + app tables). `database_id` is the
  // Terraform-managed D1 id; Miniflare uses a local SQLite db in tests regardless.
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "eyday-paper",
      "database_id": "ee5718bd-74e2-476e-acc5-3025b7899cf0",
      "migrations_dir": "src/db/migrations"
    }
  ],
  // PDFs, extracted structured text, figures. Egress-free; read repeatedly.
  "r2_buckets": [
    {
      "binding": "BUCKET",
      "bucket_name": "eyday-paper"
    }
  ],
  // Cache for AI Gateway-adjacent reuse and cheap lookups.
  "kv_namespaces": [
    {
      "binding": "KV",
      "id": "dc6142a9c64d48b5a49d39ddd8402f25"
    }
  ],
  // RAG vector search (dense + filter by userId/paperId). Used unconditionally
  // (D1 brute-force only exists as a port-level fallback for resilience).
  "vectorize": [
    {
      "binding": "VECTORIZE",
      "index_name": "eyday-paper-chunks"
    }
  ],
  // Workers AI: bge-m3 (embeddings, dense+sparse) and bge-reranker-base (rerank).
  "ai": {
    "binding": "AI"
  },
  // Async ingestion/embedding jobs: producer (request path) + consumer (pipeline).
  "queues": {
    "producers": [
      {
        "binding": "INGEST_QUEUE",
        "queue": "eyday-paper-ingest"
      }
    ],
    "consumers": [
      {
        "queue": "eyday-paper-ingest",
        "max_batch_size": 5,
        "max_batch_timeout": 30,
        "max_retries": 3
      }
    ]
  },
  // Daily suggestion batch (07:00 JST = 22:00 UTC). Caches results to D1.
  "triggers": {
    "crons": ["0 22 * * *"]
  }
}
